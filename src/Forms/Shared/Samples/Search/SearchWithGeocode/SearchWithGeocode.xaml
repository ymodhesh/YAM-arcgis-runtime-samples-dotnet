<ContentPage x:Class="ArcGISRuntimeXamarin.Samples.SearchWithGeocode.SearchWithGeocode"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Xamarin.Forms;assembly=Esri.ArcGISRuntime.Xamarin.Forms"
             xmlns:mapping="clr-namespace:Esri.ArcGISRuntime.Mapping;assembly=Esri.ArcGISRuntime"
             xmlns:toolkit="clr-namespace:Esri.ArcGISRuntime.Toolkit.Xamarin.Forms;assembly=Esri.ArcGISRuntime.Toolkit.Xamarin.Forms">
    <Grid>
        <Grid RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <esriUI:MapView x:Name="MyMapView"
                            Grid.Row="1"
                            Grid.RowSpan="2"
                            GeoViewTapped="MyMapView_GeoViewTapped" />
            <toolkit:SearchView x:Name="MySearchView"
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                GeoView="{Binding Source={Reference MyMapView}}" />
        </Grid>
    </Grid>
</ContentPage>