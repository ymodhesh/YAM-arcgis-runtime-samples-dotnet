<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"             
             xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Maui;assembly=Esri.ArcGISRuntime.Maui"
             xmlns:sampleViewer="clr-namespace:ArcGISRuntimeMaui.Resources"
             x:Class="ArcGISRuntime.Samples.DisplayDrawingStatus.DisplayDrawingStatus"
             xmlns:cmp="clr-namespace:Microsoft.Maui.Controls.Compatibility;assembly=Microsoft.Maui.Controls">
    <cmp:RelativeLayout>
        <esriUI:MapView x:Name="myMapView" Style="{StaticResource MapWithFormStyle}" ViewInsets="0" />
        <sampleViewer:ResponsiveFormContainer x:Name="FormContainer">
            <StackLayout>
                <Label Text="Pan and zoom to see drawing status changes." Style="{StaticResource LabelStyle}" />
                <ActivityIndicator x:Name="activityIndicator" Color="Black" />
            </StackLayout>
        </sampleViewer:ResponsiveFormContainer>
    </cmp:RelativeLayout>
</ContentPage>